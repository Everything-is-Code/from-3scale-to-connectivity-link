= From 3scale to Connectivity Link

*Migration Workshop — Red Hat Developer Hub · Powered by https://kuadrant.io[Kuadrant]*

[.lead]
*Total duration*: ~3–3.5 hours core path · +~1 hour optional modules (VM, Console, Migration Toolkit, APIShift)

== Welcome

In this workshop you will learn how to migrate API management from *Red Hat 3scale API Management* to *Red Hat Connectivity Link* (Kuadrant), using *Red Hat Developer Hub* as your self-service developer portal.

The cluster has two sets of pre-deployed applications — one secured by 3scale and one secured by Connectivity Link — running side by side so you can compare them directly.

NOTE: URLs that contain `\{cluster_domain}` or `apps.cluster.example.com` are *placeholders*. On a *deployed showroom* they are replaced automatically with your cluster ingress domain. On https://maximilianopizarro.github.io/from-3scale-to-connectivity-link/[GitHub Pages] they remain examples and will not resolve — use this site as documentation preview only.

IMPORTANT: Workshop usernames and passwords are *ephemeral lab credentials*. They exist only for this disposable environment. Do not reuse the same password patterns or share production secrets in training materials.

== What you will learn

* Understand *why* organizations migrate from 3scale to Connectivity Link
* Explore the architecture: Developer Hub, ArgoCD, Keycloak, Kuadrant, 3scale, Istio
* Inspect two 3scale-secured applications (OIDC and API Key)
* Inspect the equivalent Connectivity Link applications
* Compare authentication, rate limiting, GitOps, and developer experience side by side
* *Run the migration* using a generic Developer Hub Software Template
* Operate day-2 APIs with *Kuadrant Console*, explore *Migration Toolkit*, and *APIShift* (same migration path as the toolkit, plus AI models)
* *(Optional)* provision a RHEL VM behind Connectivity Link using API key auth via another Software Template
* Verify the migrated application and test both environments

== Pre-deployed scenarios

[cols="3,2,2,2",options="header"]
|===
| Application | Auth Model | 3scale Namespace | Connectivity Link Namespace

| *Neuralbank* (banking API)
| OIDC (Keycloak)
| `neuralbank-3scale`
| `neuralbank-stack`

| *NFL Wallet* (wallet API)
| API Key
| `nfl-wallet-3scale`
| `nfl-wallet-prod`
|===

== Environment access

Your credentials:

* *Username*: `{user_name}`
* *Password*: `{user_password}`

Cluster sizing and recommended profiles: xref:00-prerequisites.adoc[0. Prerequisites & Sizing].

IMPORTANT: Treat `{user_name}` / `{user_password}` as lab-only accounts. Rotate or discard them after the session.

== Workshop platform links

[cols="1,3",options="header"]
|===
| Tool | URL

| *Developer Hub*
| https://backstage-developer-hub-developer-hub.{cluster_domain}

| *Gitea*
| https://gitea-gitea.{cluster_domain}

| *ArgoCD*
| https://openshift-gitops-server-openshift-gitops.{cluster_domain}

| *Keycloak*
| https://rhbk.{cluster_domain}/admin/master/console

| *Grafana*
| https://grafana-observability.{cluster_domain}

| *Kiali*
| https://kiali-openshift-cluster-observability-operator.{cluster_domain}

| *Mailpit*
| https://n8n-mailpit-openshift-lightspeed.{cluster_domain}

| *OpenShift Console*
| https://console-openshift-console.{cluster_domain}
|===

TIP: Use the *Terminal* tab for `oc` / `curl` in this showroom (it loads the embedded terminal at `/terminal/`). The *Developer Hub* tab loads Backstage in a panel; if you see a blank or broken page (OIDC redirects often block iframe login), open Developer Hub in a new browser tab from *Workshop platform links* below instead.

== Workshop flow

image::workshop-flow.svg[Workshop flow from Developer Hub through Gitea and Argo CD to OpenShift and Kuadrant policies]

Text alternative — workshop flow steps:

. Start in *Developer Hub* (Software Templates / catalog).
. Scaffold or migrate — manifests land in *Gitea*.
. *Argo CD* syncs desired state to OpenShift.
. *Istio Gateway* + *Kuadrant* policies (AuthPolicy / RateLimitPolicy / PlanPolicy) enforce traffic.
. Verify with curl / Swagger and optional day-2 UIs (Kuadrant Console, Migration Toolkit, APIShift).

== Start here

. Complete xref:00-prerequisites.adoc[0. Prerequisites & Sizing] (~10 min).
. Continue with xref:01-migration-overview.adoc[1. Migration Overview].

If something breaks later, see xref:14-troubleshooting.adoc[14. Troubleshooting].
